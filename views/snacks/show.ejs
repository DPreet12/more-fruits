<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Snacks</title>
</head>
<body>
    <h1><%= snack.name %></h1>
    <p>Type: <%= snack.type %></p>
    <% if (snack.readyToEat) { %>
        <p>Ready to Eat 👍🏽</p>
    <% } else { %>
        <p>Not Ready to Eat 👎🏽</p>
    <% } %>

        <a href="/snacks">Go back.....</a>
        <button><a href="/snacks/<%= id %>/edit">Edit</a></button>
        <button><a href="/snacks/<%= id %>/delete">Delete</a></button>
</body>
</html>
